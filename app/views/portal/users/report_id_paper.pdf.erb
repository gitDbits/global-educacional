<html>
  <head>
    <title>Etiquetas</title>
    <style>
      @page {
        size: A4;
        margin: 9mm 7mm; /* Margens superior e inferior de 9mm e laterais de 7mm */
      }
      body {
        font-family: "Arial", sans-serif;
        font-size: 10pt;
        margin: 0;
        padding: 0;
      }
      .container {
        width: 100%;
        padding-top: 9mm; /* Margem superior */
      }
      .row {
        clear: both;
        content: "";
        display: table;
      }
      .etiqueta {
        width: 63.5mm;
        height: 31mm;
        float: left;
        box-sizing: border-box;
        padding: 2mm;
        margin-right: 7mm; /* Espaçamento horizontal entre as etiquetas */
        margin-bottom: 9mm; /* Espaçamento vertical entre as filas */
      }
      .etiqueta:nth-child(3n) {
        margin-right: 0; /* Remover a margem da direita na última etiqueta de cada linha */
      }
    </style>
  </head>
  <body>
    <div class="container">
      <!-- Substitua este bloco com o loop adequado para gerar as 27 etiquetas -->
      <!-- Início do loop de etiquetas -->
      <div class="etiqueta">Etiqueta 1</div>
      <div class="etiqueta">Etiqueta 2</div>
      <div class="etiqueta">Etiqueta 3</div>
      <!-- A "row" é apenas usada para limpar o float -->
      <div class="row"></div>
      <div class="etiqueta">Etiqueta 4</div>
      <!-- Repita para todas as 27 etiquetas -->
      <!-- Fim do loop de etiquetas -->
    </div>
  </body>
</html>
